#include<stdio.h>
#include<string.h>

int count_words(int string[]){
    int length = strlen(string);

    retrun length;
}

int main(){
    char string[100];
    scanf("%s",string);

    printf("%d",count_words(string));
    return 0;
}